# =============================================================================
# OpenEdge Analytics MCP Server - 환경 설정 파일
# =============================================================================
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요.
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# ODBC 데이터베이스 연결 설정
# -----------------------------------------------------------------------------

# ODBC DSN 이름 (시스템 DSN 또는 사용자 DSN)
# Windows: ODBC 데이터 원본 관리자에서 설정
# Linux: /etc/odbc.ini 또는 ~/.odbc.ini에서 설정
ODBC_DSN=QAD_OPENEDGE_ANALYTICS

# 데이터베이스 사용자 이름
DB_USER=your_username

# 데이터베이스 비밀번호
DB_PASSWORD=your_password

# 연결 타임아웃 (초 단위)
DB_TIMEOUT_SECONDS=30

# -----------------------------------------------------------------------------
# 스키마 설정
# -----------------------------------------------------------------------------

# QAD 스키마 이름
# SQL 템플릿의 {schema} placeholder가 이 값으로 치환됩니다.
# QAD OpenEdge 기본 스키마는 PUB입니다.
ANALYTICS_SCHEMA=PUB
